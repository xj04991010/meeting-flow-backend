name: Daily AI Assistant Cron Jobs

on:
  schedule:
    # --- Wake up pings (10 minutes before actual jobs) ---
    # 00:50 UTC = 08:50 Taipei
    - cron: '50 0 * * *'
    # 06:50 UTC = 14:50 Taipei
    - cron: '50 6 * * *'
    # 11:50 UTC = 19:50 Taipei
    - cron: '50 11 * * *'
    
    # --- Actual Cron Jobs ---
    # Morning Briefing: 01:00 UTC = 09:00 Taipei
    - cron: '0 1 * * *'
    # Afternoon Nudging: 07:00 UTC = 15:00 Taipei
    - cron: '0 7 * * *'
    # Evening Report: 12:00 UTC = 20:00 Taipei
    # Proactive Agent: 19:00 UTC = 03:00 Taipei (Next Day)
    - cron: '0 19 * * *'
    # Weekly Memory Decay: Sunday 18:00 UTC = Monday 02:00 Taipei
    - cron: '0 18 * * 0'
  
  workflow_dispatch:
    inputs:
      action:
        description: 'Select exactly one cron action to run'
        required: true
        default: 'wake_up'
        type: choice
        options:
          - wake_up
          - morning
          - nudging
          - evening
          - proactive
          - weekly

jobs:
  run_crons:
    runs-on: ubuntu-latest
    steps:
      - name: Calculate Time and Determine Action
        id: cron_action
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            case "${{ inputs.action }}" in
              wake_up) echo "ACTION=WAKE_UP" >> $GITHUB_ENV ;;
              morning) echo "ACTION=MORNING" >> $GITHUB_ENV ;;
              nudging) echo "ACTION=NUDGING" >> $GITHUB_ENV ;;
              evening) echo "ACTION=EVENING" >> $GITHUB_ENV ;;
              proactive) echo "ACTION=PROACTIVE" >> $GITHUB_ENV ;;
              weekly) echo "ACTION=WEEKLY" >> $GITHUB_ENV ;;
              *) echo "Unknown manual action"; exit 1 ;;
            esac
            exit 0
          fi

          HOUR=$(date -u +"%H")
          MINUTE=$(date -u +"%M")
          DAY=$(date -u +"%w")
          
          if [ "$MINUTE" = "50" ]; then
            echo "ACTION=WAKE_UP" >> $GITHUB_ENV
            echo "Endpoint: GET /"
          elif [ "$HOUR" = "01" ]; then
            echo "ACTION=MORNING" >> $GITHUB_ENV
            echo "Endpoint: POST /api/cron/morning"
          elif [ "$HOUR" = "07" ]; then
            echo "ACTION=NUDGING" >> $GITHUB_ENV
            echo "Endpoint: POST /api/cron/nudging"
          elif [ "$HOUR" = "12" ]; then
            echo "ACTION=EVENING" >> $GITHUB_ENV
            echo "Endpoint: POST /api/cron/evening"
          elif [ "$HOUR" = "19" ]; then
            echo "ACTION=PROACTIVE" >> $GITHUB_ENV
            echo "Endpoint: POST /api/cron/proactive"
          elif [ "$HOUR" = "18" ] && [ "$DAY" = "0" ]; then
            echo "ACTION=WEEKLY" >> $GITHUB_ENV
            echo "Endpoint: POST /api/cron/weekly"
          else
            echo "ACTION=NOOP" >> $GITHUB_ENV
          fi

      - name: Execute Wake Up Ping
        if: env.ACTION == 'WAKE_UP'
        run: curl -s https://meeting-flow-backend.onrender.com/ > /dev/null

      - name: Execute Morning Briefing
        if: env.ACTION == 'MORNING'
        run: |
          curl -X POST \
            -H "x-cron-token: ${{ secrets.CRON_SECRET }}" \
            --max-time 120 \
            --fail \
            https://meeting-flow-backend.onrender.com/api/cron/morning

      - name: Execute Afternoon Nudging
        if: env.ACTION == 'NUDGING'
        run: |
          curl -X POST \
            -H "x-cron-token: ${{ secrets.CRON_SECRET }}" \
            --max-time 120 \
            --fail \
            https://meeting-flow-backend.onrender.com/api/cron/nudging

      - name: Execute Evening Report
        if: env.ACTION == 'EVENING'
        run: |
          curl -X POST \
            -H "x-cron-token: ${{ secrets.CRON_SECRET }}" \
            --max-time 120 \
            --fail \
            https://meeting-flow-backend.onrender.com/api/cron/evening

      - name: Execute Proactive Agent
        if: env.ACTION == 'PROACTIVE'
        run: |
          curl -X POST \
            -H "x-cron-token: ${{ secrets.CRON_SECRET }}" \
            --max-time 120 \
            --fail \
            https://meeting-flow-backend.onrender.com/api/cron/proactive

      - name: Execute Weekly Memory Decay
        if: env.ACTION == 'WEEKLY'
        run: |
          curl -X POST \
            -H "x-cron-token: ${{ secrets.CRON_SECRET }}" \
            --max-time 120 \
            --fail \
            https://meeting-flow-backend.onrender.com/api/cron/weekly
